package model;

/**
 * Represents the hero class/role types in Honor of Kings.
 * Used to categorize heroes and determine equipment compatibility.
 */
public enum HeroType {
    MAGE,
    ASSASSIN,
    TANK,
    WARRIOR,
    MARKSMAN,
    SUPPORT
}
